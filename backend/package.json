{
  "name": "basic_motoko_backend",
  "version": "1.0.0",
  "description": "A basic Motoko canister backend",
  "scripts": {
    "build": "dfx build",
    "deploy": "dfx deploy",
    "start": "dfx start --clean",
    "stop": "dfx stop"
  },
  "dependencies": {},
  "devDependencies": {
    "dfx": "^0.15.0"
  }
}